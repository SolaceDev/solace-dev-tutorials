import React from "react"

const sampleTemplate = () => (
  <div>
    <h1>Sample Title Here</h1>
    <p>
      This template will contain the content from individual Markdown files and
      will contain a sidebar with the links to the other pages (pulled in from
      the tutorials.yml).
    </p>
  </div>
)

export default sampleTemplate
